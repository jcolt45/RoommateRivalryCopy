{
  "dependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.5",
    "react-bootstrap": "^2.10.1"
  },
  "scripts": {
    "format": "npx prettier . --write",
    "lint": "npx eslint ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
