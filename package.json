{
  "dependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.5",
    "react-bootstrap": "^2.10.1"
  },
  "scripts": {
    "format": "npx prettier . --write",
    "lint": "npx eslint .",
    "front-start": "cd packages/frontend && npm run start",
    "back-start": "cd packages/express-backend && npm run start",
    "back-start-dev": "cd packages/express-backend && npm run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
